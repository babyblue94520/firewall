logging:
  level:
    org.gradle: debug
management:
  security:
    enabled: false
spring:
  application:
    name: spring-cloud-firewall
  profiles:
    active: firewall
  config:
    name: application
    location: configs/
  devtools:
    restart:
      enabled: true
      poll-interval: 5000
      quiet-period: 500